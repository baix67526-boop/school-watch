name: school watch

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"
  push:
    paths:
      - sources.txt
      - watcher.py
      - subscriptions.xlsx
      - state.json
      - .github/workflows/watch.yml

jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/check
